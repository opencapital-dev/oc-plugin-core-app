{
  "type": "timeseries",
  "title": "Beta over time",
  "description": "Rolling beta (last $lookback_periods periods) and expanding beta (since window start), at every sample-interval tick.",
  "fieldConfig": {
    "defaults": {
      "unit": "none",
      "decimals": 2,
      "custom": {
        "drawStyle": "line",
        "lineWidth": 2,
        "fillOpacity": 0,
        "showPoints": "never",
        "spanNulls": false
      },
      "color": {
        "mode": "palette-classic"
      }
    },
    "overrides": [
      {
        "matcher": {
          "id": "byName",
          "options": "ts"
        },
        "properties": [
          {
            "id": "displayName",
            "value": "time"
          }
        ]
      },
      {
        "matcher": {
          "id": "byName",
          "options": "rolling"
        },
        "properties": [
          {
            "id": "displayName",
            "value": "Rolling beta"
          }
        ]
      },
      {
        "matcher": {
          "id": "byName",
          "options": "expanding"
        },
        "properties": [
          {
            "id": "displayName",
            "value": "Expanding beta"
          }
        ]
      }
    ]
  },
  "options": {
    "tooltip": {
      "mode": "multi",
      "sort": "none"
    },
    "legend": {
      "displayMode": "table",
      "placement": "bottom",
      "calcs": [
        "last",
        "mean",
        "min",
        "max"
      ]
    }
  },
  "targets": [
    {
      "refId": "A",
      "datasource": {
        "type": "core-datasource",
        "uid": "core-datasource"
      },
      "ref": "core-app/beta_series"
    }
  ],
  "datasource": {
    "type": "core-datasource",
    "uid": "core-datasource"
  }
}
