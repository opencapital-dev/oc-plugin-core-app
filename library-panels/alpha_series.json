{
  "type": "timeseries",
  "title": "Alpha (annualized) over time",
  "description": "Per-period OLS intercept geometrically annualized. Rolling = last $lookback_periods periods.",
  "fieldConfig": {
    "defaults": {
      "unit": "percentunit",
      "decimals": 2,
      "custom": {
        "drawStyle": "line",
        "lineWidth": 2,
        "fillOpacity": 0,
        "showPoints": "never",
        "spanNulls": false
      },
      "color": {
        "mode": "palette-classic"
      }
    },
    "overrides": [
      {
        "matcher": {
          "id": "byName",
          "options": "ts"
        },
        "properties": [
          {
            "id": "displayName",
            "value": "time"
          }
        ]
      },
      {
        "matcher": {
          "id": "byName",
          "options": "rolling"
        },
        "properties": [
          {
            "id": "displayName",
            "value": "Rolling alpha (ann)"
          }
        ]
      },
      {
        "matcher": {
          "id": "byName",
          "options": "expanding"
        },
        "properties": [
          {
            "id": "displayName",
            "value": "Expanding alpha (ann)"
          }
        ]
      }
    ]
  },
  "options": {
    "tooltip": {
      "mode": "multi",
      "sort": "none"
    },
    "legend": {
      "displayMode": "table",
      "placement": "bottom",
      "calcs": [
        "last",
        "mean",
        "min",
        "max"
      ]
    }
  },
  "targets": [
    {
      "refId": "A",
      "datasource": {
        "type": "core-datasource",
        "uid": "core-datasource"
      },
      "ref": "core-app/alpha_series"
    }
  ],
  "datasource": {
    "type": "core-datasource",
    "uid": "core-datasource"
  }
}
