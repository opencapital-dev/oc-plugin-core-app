{
  "type": "stat",
  "title": "Avg loss",
  "description": "Mean PnL across losing closures, base ccy (negative).",
  "fieldConfig": {
    "defaults": {
      "unit": "locale",
      "decimals": 0,
      "color": {
        "mode": "fixed",
        "fixedColor": "red"
      }
    }
  },
  "options": {
    "colorMode": "value",
    "graphMode": "none",
    "reduceOptions": {
      "calcs": [
        "lastNotNull"
      ],
      "fields": "",
      "values": false
    },
    "text": {
      "titleSize": 16,
      "valueSize": 32
    },
    "textMode": "auto"
  },
  "targets": [
    {
      "refId": "A",
      "datasource": {
        "type": "core-datasource",
        "uid": "core-datasource"
      },
      "source": "@bind(closures=\"closures{portfolio=\\\"$portfolio_id\\\"} @window\")\n@metric(output=\"scalar\")\ndef avg_loss(closures):\n    losses = closures.filter(pl.col(\"realized_pnl\") < 0)\n    return None if losses.is_empty() else float(losses[\"realized_pnl\"].mean())\n"
    }
  ],
  "datasource": {
    "type": "core-datasource",
    "uid": "core-datasource"
  }
}
