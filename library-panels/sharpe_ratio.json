{
  "type": "stat",
  "title": "Sharpe Ratio",
  "description": "Mean(excess) / stdev(excess) \u00d7 sqrt(periods/year). Excess = period return \u2212 rf_per_period.",
  "fieldConfig": {
    "defaults": {
      "unit": "none",
      "decimals": 2,
      "color": {
        "mode": "thresholds"
      },
      "thresholds": {
        "mode": "absolute",
        "steps": [
          {
            "color": "red",
            "value": null
          },
          {
            "color": "yellow",
            "value": 0.5
          },
          {
            "color": "green",
            "value": 1.0
          }
        ]
      }
    }
  },
  "options": {
    "colorMode": "value",
    "graphMode": "none",
    "reduceOptions": {
      "calcs": [
        "lastNotNull"
      ],
      "fields": "",
      "values": false
    },
    "text": {
      "titleSize": 16,
      "valueSize": 32
    },
    "textMode": "auto"
  },
  "targets": [
    {
      "refId": "A",
      "datasource": {
        "type": "core-datasource",
        "uid": "core-datasource"
      },
      "source": "STEP_SEC = 3600 if \"${sample_interval}\" == \"1h\" else 86400\nSTEP_US = STEP_SEC * 1_000_000\nRISK_FREE = ${risk_free}\n\n@bind(nav=\"nav{portfolio=\\\"$portfolio_id\\\"} @asof\", flows=\"flows{portfolio=\\\"$portfolio_id\\\"} @window\")\n@metric(output=\"scalar\")\ndef sharpe_ratio(nav, flows):\n    t0, t1 = window\n    effective_start = t0 if nav.is_empty() else max(t0, nav[\"ts\"][0])\n    flow_ts = [r[0] for r in flows.select(\"ts\").rows() if r[0] > effective_start]\n    grid = build_grid(effective_start, t1, STEP_US)\n    cum_series = cumulative_twr(nav, flow_ts, effective_start, grid)\n    if not cum_series:\n        return None\n    cum = [v for _, v in cum_series]\n    period_rs = cum_to_period_returns(cum)\n    py = (365 * 86400) / STEP_SEC\n    return sharpe(period_rs, RISK_FREE, py)\n"
    }
  ],
  "datasource": {
    "type": "core-datasource",
    "uid": "core-datasource"
  }
}
